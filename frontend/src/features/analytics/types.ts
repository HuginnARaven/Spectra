export type UrlVisitData = {
    id: string
    ipAddress: string
    country: string
    city: string
    userAgent: string
    browser: string
    deviceType: string
    referrer: string
    createdAt: string
}
